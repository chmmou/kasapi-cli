// Package symlink will hold the domain types and use cases for the KAS
// symlink endpoint (add_symlink).
//
// Not yet implemented: this package is an intentional placeholder so the
// captured fixtures under testdata/symlink/ have a home before the slice
// is built. There is no mapping, client, or CLI surface yet. Tracked by
// issue #13 (ROADMAP: "Filesystem helpers").
package symlink
